/* REXX */
ARG VOL
IF ISFCALLS('ON') <> 0 THEN EXIT 99
COMMAND='D U,VOL='VOL''
ADDRESS SDSF "ISFEXEC '/"COMMAND"' (WAIT"
/* SPLITTING STRINGS TO VOLS */
ADDR=SUBSTR(ISFULOG.5,44,4)
CALL ISFCALLS 'OFF'
RETURN ADDR
